class reverseArray():
    def reverseArray(self, arr):
        #start:stop:step notation. Stepping back by one spot
        #each time and python assumes start is at the end of
        #array and stop is at beginning since -1 is step.
        return arr[::-1]
    
